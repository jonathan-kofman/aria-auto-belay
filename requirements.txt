# ARIA dashboard and tools — install with: pip install -r requirements.txt
# Use this with .python\ (START_DASHBOARD.bat) or system Python.

streamlit
pandas
numpy
plotly
pyserial
sounddevice
soundfile
firebase-admin
