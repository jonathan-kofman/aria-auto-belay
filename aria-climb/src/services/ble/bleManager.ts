// TODO: Phase 2 — BleManager singleton, scan, connect, disconnect
export const bleManager = {};
export function scan(): Promise<void> {
  return Promise.resolve();
}
export function connect(_deviceId: string): Promise<void> {
  return Promise.resolve();
}
export function disconnect(_deviceId: string): Promise<void> {
  return Promise.resolve();
}
