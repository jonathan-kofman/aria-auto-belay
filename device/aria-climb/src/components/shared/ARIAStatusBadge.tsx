import React from 'react';
import { View, Text } from 'react-native';
// TODO: Phase 2 — IDLE / CLIMBING / PAUSED / ALERT badge

export function ARIAStatusBadge() {
  return <View><Text>ARIAStatusBadge</Text></View>;
}
