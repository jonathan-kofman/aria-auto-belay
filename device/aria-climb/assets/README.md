# Assets

Add the following for Expo to build:

- **icon.png** — App icon (e.g. 1024×1024)
- **splash.png** — Splash screen image

Or remove/comment the `icon` and `splash` entries in `app.json` to use Expo defaults.
